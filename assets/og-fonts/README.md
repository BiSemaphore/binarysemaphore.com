# Share image fonts

Static woff files for `next/og`, which cannot use `next/font` and does not read
woff2. Used by `src/app/team/[slug]/opengraph-image.tsx`.

- `plus-jakarta-sans-latin-700-normal.woff` from `@fontsource/plus-jakarta-sans` 5.3.0
- `inter-tight-latin-*-normal.woff` from `@fontsource/inter-tight` 5.3.0

Both are licensed under the SIL Open Font License 1.1.
