| Behaviour | Because the index is… |
|---|---|
| Hot partitions from sequential keys (section 15) | partitioned by <Pink>key range</Pink> |
| LIST is slow and paginated (section 16) | a <Peach>range scan</Peach>, not a lookup |
| No atomic rename (section 16) | a key change <Peach>moves partitions</Peach> |
| `HEAD` is instant and touches no drive | size and ETag <Peach>live in the index</Peach> |
| Folders are a group-by (section 14) | a <Peach>sorted</Peach> structure, so prefixes are contiguous ranges |

And it must be <Peach>strongly consistent</Peach>, which is the genuinely hard requirement:
trillions of keys, globally distributed, millisecond lookups, and a write must
be visible to the next read.
