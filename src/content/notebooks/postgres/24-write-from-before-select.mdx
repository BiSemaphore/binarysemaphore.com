---
number: "24"
title: "Write `FROM` before `SELECT`"
part: "V"
partTitle: "Querying for an API"
---
SQL is written `SELECT ... FROM ...` and should be <Peach>thought</Peach> in the other
order. Decide where the rows come from, then decide which columns you want.
